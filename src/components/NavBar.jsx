const NavBar = () => {
    return (

        <nav className="navbar">
            <h1>Weather App</h1>
            <div className="container__btn--darkmode">
                
            </div>
        </nav>
    );
};

export default NavBar;